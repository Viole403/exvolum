import { NextRequest, NextResponse } from 'next/server';

export async function POST(req: NextRequest) {
  // Placeholder: implement user sync logic here
  return NextResponse.json({ message: 'User sync endpoint is working.' });
}
